// import { Router } from 'express';
// import { createCategory, getAllCategories, getCategoryById, updateCategory, deleteCategory } from '../controllers/categoryProductController.ts';
// import { superAdminOnly, storeAdminOnly } from '../middlewares/isSuperAdmin.ts';
// import { validateCategory } from '../middlewares/validateCategory.ts';

// const router = Router();

// router.get('/categories', storeAdminOnly, getAllCategories);
// router.get('/categories', storeAdminOnly, getCategoryById);
// router.post('/categories', superAdminOnly, validateCategory, createCategory);
// router.put('/categories/:id', superAdminOnly, validateCategory, updateCategory);
// router.delete('/categories/:id', superAdminOnly, deleteCategory);

// export default router;
